# Test Environment Configuration
# Copy this file to .env.test and configure for your test environment

# Database Configuration (PostgreSQL)
TEST_DB_USER=test_user
TEST_DB_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD
TEST_DB_NAME=spheroseg_test

# JWT Configuration (Generate with: openssl rand -hex 32)
TEST_JWT_ACCESS_SECRET=GENERATE_ME_WITH_OPENSSL_RAND_HEX_32
TEST_JWT_REFRESH_SECRET=GENERATE_ME_WITH_OPENSSL_RAND_HEX_32

# Note: For CI environments, these can be set as secrets in your CI/CD platform