# Core dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
# requests 2.32.4+ fixes CVE-2024-47081 (.netrc credential leak)
# and CVE-2026-25645 (insecure temp file reuse in extract_zipped_paths).
requests==2.32.4
# python-multipart 0.0.18+ fixes CVE-2024-24762 (Content-Type header
# ReDoS), CVE-2024-53981 (multipart boundary DoS), and CVE-2026-24486
# (arbitrary file write via non-default config).
python-multipart==0.0.18
psutil==5.9.5

# ML dependencies (CUDA variant specified in Dockerfiles)
# torch 2.6.0+ fixes CVE-2025-32434 (torch.load RCE even with
# weights_only=True). Also addresses CVE-2025-3730 (improper resource
# shutdown) and CVE-2025-2953 (local DoS). Pair with torchvision 0.21.0
# which is the compatible release for torch 2.6.x.
torch==2.6.0
torchvision==0.21.0

# Image processing
opencv-python-headless==4.8.1.78
# Pillow 10.4.0 fixes CVE-2023-50447 (ImageMath.eval arbitrary code
# execution) and CVE-2024-28219 (buffer overflow in c_memcpy).
Pillow==10.4.0
numpy==1.24.3
scipy==1.11.4
scikit-image==0.22.0
# Sperm model dependencies (Mask2Former + graph assembly)
# v17: DINOv2-L/14 backbone via HuggingFace's own transformers.Dinov2Model
# (no timm runtime dependency). On first inference the HF cache is
# populated (~1.3 GB); mount HF_HOME as a volume to persist across
# container restarts.
networkx>=3.0
transformers>=4.40.0

# Wound-healing model (U-Net++ / ResNeXt-50 via segmentation_models_pytorch)
segmentation-models-pytorch==0.3.4

# Prometheus metrics
prometheus-fastapi-instrumentator==6.1.0
