# REST API reference

Every endpoint the Node backend exposes, grouped by resource. This page is
generated by hand from the route files and is checked against them; where an
older doc disagrees, this page and the code are right.

- **Base URL**: `https://spherosegapp.utia.cas.cz/api` in production,
  `http://localhost:3001/api` in development.
- **Interactive spec**: Swagger UI at `http://localhost:3001/api-docs` in
  development. See [Swagger / OpenAPI](swagger-openapi.md).
- **Authentication**: [Authentication](authentication.md).
- **Real-time**: [WebSocket events](websocket.md).
- **ML service** (a separate FastAPI process, not usually called directly):
  [ML service API](ml-service.md).

Unless stated otherwise every endpoint requires authentication and returns JSON.

---

## Conventions

**Errors** carry an HTTP status and a JSON body with a message. Common codes:

| Code | Meaning here                                                                                    |
| ---- | ----------------------------------------------------------------------------------------------- |
| 400  | Validation failure, or a request that is well-formed but not allowed in the current state       |
| 401  | Not authenticated, or the access token expired — refresh and retry                              |
| 403  | Authenticated but not permitted                                                                 |
| 404  | Not found **or not yours** — the two are deliberately indistinguishable so ids cannot be probed |
| 429  | Rate limited, or a per-user concurrency limit (exports allow one at a time)                     |

**Pagination** is by `limit` and `offset` query parameters where a list can grow.

**Validation** is schema-based on both params and body. Note an implementation
detail that matters if you are editing routes: the params validator _replaces_
`req.params` with the parsed object, so a schema must declare **every** param
the handler reads, not just the one being checked.

---

## Health and status

| Method | Path                                | Auth | Purpose                                     |
| ------ | ----------------------------------- | :--: | ------------------------------------------- |
| GET    | `/api/health`                       |  no  | Overall health summary                      |
| GET    | `/api/health/live`                  |  no  | Liveness — always 200 if the process is up  |
| GET    | `/api/health/ready`                 |  no  | Readiness, with the list of blocking issues |
| GET    | `/api/health/detailed`              |  no  | Per-component detail                        |
| GET    | `/api/health/components/:component` |  no  | One component's health                      |
| POST   | `/api/health/check`                 |  no  | Force a health re-check                     |
| GET    | `/api/ml/status`                    | yes  | ML service status as seen from the backend  |
| GET    | `/api/ml/health`                    | yes  | ML service health passthrough               |

---

## Authentication

| Method | Path                               |  Auth  | Purpose                                           |
| ------ | ---------------------------------- | :----: | ------------------------------------------------- |
| POST   | `/api/auth/register`               |   no   | Create an account. Rate limited                   |
| POST   | `/api/auth/login`                  |   no   | Sign in; sets the auth cookies. Rate limited      |
| POST   | `/api/auth/refresh`                | cookie | Exchange the refresh token for a new access token |
| POST   | `/api/auth/refresh-token`          | cookie | Alias of the above                                |
| POST   | `/api/auth/logout`                 |  yes   | Invalidate the session and clear cookies          |
| POST   | `/api/auth/request-password-reset` |   no   | Send a reset e-mail. Strictly rate limited        |
| POST   | `/api/auth/forgot-password`        |   no   | Alias of the above                                |
| POST   | `/api/auth/reset-password`         |   no   | Complete a reset with the token                   |
| GET    | `/api/auth/verify-email/:token`    |   no   | Verify an e-mail address                          |
| POST   | `/api/auth/resend-verification`    |   no   | Re-send the verification e-mail                   |
| GET    | `/api/auth/check`                  | cookie | Cheap "am I signed in?" probe                     |
| GET    | `/api/auth/profile`                |  yes   | The signed-in user's profile                      |
| PUT    | `/api/auth/profile`                |  yes   | Update the profile                                |
| POST   | `/api/auth/change-password`        |  yes   | Change the password                               |
| POST   | `/api/auth/avatar`                 |  yes   | Upload an avatar image                            |
| GET    | `/api/auth/storage-stats`          |  yes   | Storage used by this account                      |

## Users

A second surface over the same data, used by the settings pages.

| Method | Path                         | Purpose                                                   |
| ------ | ---------------------------- | --------------------------------------------------------- |
| GET    | `/api/users/profile`         | Profile                                                   |
| PUT    | `/api/users/profile`         | Update profile                                            |
| POST   | `/api/users/change-password` | Change password                                           |
| GET    | `/api/users/settings`        | Preferences (model, threshold, language, theme, consents) |
| GET    | `/api/users/storage-stats`   | Storage usage                                             |
| GET    | `/api/users/activity`        | Recent activity, `limit` + `offset`                       |
| DELETE | `/api/users/account`         | **Delete the account and all its data**                   |

---

## Projects

| Method | Path                         | Purpose                                                             |
| ------ | ---------------------------- | ------------------------------------------------------------------- |
| POST   | `/api/projects`              | Create a project (`title`, `description?`, `type`)                  |
| GET    | `/api/projects`              | List the caller's projects; cached per user                         |
| GET    | `/api/projects/:id`          | One project; accessible to the owner **and** accepted collaborators |
| PUT    | `/api/projects/:id`          | Update title / description / type — **owner only**                  |
| DELETE | `/api/projects/:id`          | Delete the project and everything in it — owner only                |
| PATCH  | `/api/projects/:id/verified` | Set the "reviewed" flag — owner **or** accepted collaborator        |
| GET    | `/api/projects/:id/stats`    | Image and segmentation counts                                       |

`type` must be one of `spheroid`, `spheroid_invasive`, `wound`, `sperm`,
`microtubules`, `microcapsule`. See [Project types](../guides/project-types.md).

### Microtubule type-label palette

| Method | Path                                        | Purpose                                                         |
| ------ | ------------------------------------------- | --------------------------------------------------------------- |
| GET    | `/api/projects/:id/mt-type-labels`          | The project's label palette                                     |
| PUT    | `/api/projects/:id/mt-type-labels`          | Replace the palette                                             |
| DELETE | `/api/projects/:id/mt-type-labels/:labelId` | Delete one label; clears it from every microtubule that used it |

## Project folders

A per-user folder tree. The same project can sit in different folders for
different users.

| Method | Path                       | Purpose                               |
| ------ | -------------------------- | ------------------------------------- |
| GET    | `/api/folders`             | The caller's folder tree              |
| POST   | `/api/folders`             | Create a folder (`name`, `parentId?`) |
| PATCH  | `/api/folders/:id`         | Rename or re-parent                   |
| DELETE | `/api/folders/:id`         | Delete the folder and its subtree     |
| GET    | `/api/folders/:id/preview` | What deleting it would affect         |
| POST   | `/api/folders/:id/items`   | Move projects into the folder         |
| POST   | `/api/folders/root/items`  | Move projects back to the root level  |

---

## Images

Mounted twice, so the same handlers are reachable as
`/api/projects/:id/...` and as `/api/images/...`.

| Method | Path                                             |  Auth  | Purpose                                                                      |
| ------ | ------------------------------------------------ | :----: | ---------------------------------------------------------------------------- |
| GET    | `/api/images/:imageId/display`                   | **no** | The image's pixels, for `<img>`                                              |
| GET    | `/api/images/:imageId/frame-data?channel=&repr=` | **no** | One frame of one channel; `repr=proxy` requests the fast 8-bit playback copy |
| GET    | `/api/projects/:id/images`                       |  yes   | List images (paginated, filterable)                                          |
| GET    | `/api/projects/:id/images-with-thumbnails`       |  yes   | List with thumbnail URLs                                                     |
| GET    | `/api/projects/:id/images/stats`                 |  yes   | Counts by segmentation status                                                |
| GET    | `/api/projects/:projectId/images/:imageId`       |  yes   | One image                                                                    |
| GET    | `/api/images/:imageId`                           |  yes   | One image **with** its segmentation                                          |
| POST   | `/api/projects/:id/images`                       |  yes   | Upload still images (field `images`, max 100 files, 20 MB each)              |
| POST   | `/api/projects/:id/videos`                       |  yes   | Upload one video / ND2 / TIFF stack (field `video`, 100 GB)                  |
| POST   | `/api/projects/:id/images/add-channel`           |  yes   | Add a channel to selected frames (microtubule projects)                      |
| PATCH  | `/api/projects/:id/images/reorder`               |  yes   | Set display order (time series)                                              |
| PATCH  | `/api/images/:imageId/channels`                  |  yes   | Replace the container's channel list                                         |
| GET    | `/api/images/:imageId/video-frames`              |  yes   | Frames of a video container                                                  |
| POST   | `/api/projects/:id/regenerate-thumbnails`        |  yes   | Rebuild thumbnails                                                           |
| DELETE | `/api/projects/:projectId/images/:imageId`       |  yes   | Delete one image                                                             |
| DELETE | `/api/images/batch`                              |  yes   | Delete many by id                                                            |

> **`/display` and `/frame-data` are intentionally unauthenticated.** A browser
> `<img>` cannot carry a bearer token, so the image UUID _is_ the capability.
> Treat an image URL as equivalent to sharing that image.

> **Channel replacement is whole-array.** `PATCH /channels` replaces the list, so
> send every channel back with all of its fields, not just the one you changed.

---

## Segmentation

| Method | Path                                                | Purpose                                         |
| ------ | --------------------------------------------------- | ----------------------------------------------- |
| GET    | `/api/segmentation/images/:imageId/results`         | The stored polygons for an image                |
| DELETE | `/api/segmentation/images/:imageId/results`         | Delete them                                     |
| POST   | `/api/segmentation/batch`                           | Segment up to 50 images                         |
| POST   | `/api/segmentation/batch/results`                   | Fetch results for up to 1000 images             |
| POST   | `/api/segmentation/batch/delete`                    | Delete results for up to 10 000 images          |
| DELETE | `/api/segmentation/videos/:videoId/tracks/:trackId` | Delete a microtubule track from **every** frame |

Track propagation and per-track type assignment are also served from this
router; see [Microtubule projects](../guides/project-types/microtubules.md).

## Queue

| Method | Path                                   | Purpose                                                             |
| ------ | -------------------------------------- | ------------------------------------------------------------------- |
| POST   | `/api/queue/images/:imageId`           | Enqueue one image (`model`, `threshold`, `detectHoles`, `channel?`) |
| POST   | `/api/queue/batch`                     | Enqueue a batch                                                     |
| GET    | `/api/queue/projects/:projectId/stats` | Queue stats for a project                                           |
| GET    | `/api/queue/projects/:projectId/items` | Queued items for a project                                          |
| DELETE | `/api/queue/items/:queueId`            | Remove one item                                                     |
| POST   | `/api/queue/cancel-all-user`           | Cancel everything this user has queued                              |
| GET    | `/api/queue/stats`                     | Global queue stats                                                  |
| GET    | `/api/queue/health`                    | Queue health                                                        |
| POST   | `/api/queue/reset-stuck`               | Requeue items stuck longer than N minutes                           |
| POST   | `/api/queue/cleanup`                   | Prune finished items older than N days                              |

> **Enqueuing is not a guarantee.** Model/project-type compatibility is checked
> by the **worker** at dispatch, not at enqueue, so an accepted item can still
> be rejected.

---

## Export

| Method | Path                                                    | Purpose                                          |
| ------ | ------------------------------------------------------- | ------------------------------------------------ |
| POST   | `/api/projects/:projectId/export`                       | Start an export; returns `{ jobId }`             |
| GET    | `/api/projects/:projectId/export/:jobId/status`         | Progress and stage                               |
| POST   | `/api/projects/:projectId/export/:jobId/download-token` | A signed token valid for 10 minutes              |
| GET    | `/api/projects/:projectId/export/:jobId/download`       | The ZIP; accepts a bearer token **or** `?token=` |
| POST   | `/api/projects/:projectId/export/:jobId/cancel`         | Cancel                                           |
| GET    | `/api/projects/:projectId/export/history`               | Past exports                                     |
| GET    | `/api/export/formats`                                   | Available formats                                |

**One export at a time per user** — a second POST returns 429.

The two-step download exists because a browser download cannot carry an
authorization header. See [Export](../guides/export.md).

---

## Sharing

| Method | Path                                |   Auth   | Purpose                                |
| ------ | ----------------------------------- | :------: | -------------------------------------- |
| POST   | `/api/projects/:id/share/email`     |   yes    | Invite by e-mail                       |
| POST   | `/api/projects/:id/share/link`      |   yes    | Create a share link                    |
| GET    | `/api/projects/:id/shares`          |   yes    | List shares on a project               |
| DELETE | `/api/projects/:id/shares/:shareId` |   yes    | Revoke a share                         |
| GET    | `/api/shared/projects`              |   yes    | Projects shared **with** the caller    |
| GET    | `/api/share/validate/:token`        | optional | Inspect a share token before accepting |
| POST   | `/api/share/accept/:token`          | optional | Accept an invitation                   |

The last two accept an unauthenticated caller so the accept page can render
before sign-in. See [Sharing](../guides/sharing-and-collaboration.md).

---

## Automated Essays

| Method | Path                                     | Purpose                                                                           |
| ------ | ---------------------------------------- | --------------------------------------------------------------------------------- |
| POST   | `/api/essays/upload`                     | Upload a folder of `.nd2` (field `files`, max 512, 100 GB each) plus `folderName` |
| GET    | `/api/essays/jobs`                       | The caller's jobs, newest first, capped at 100                                    |
| GET    | `/api/essays/jobs/:jobId`                | One job, reconciled from the worker                                               |
| POST   | `/api/essays/jobs/:jobId/download-token` | A 10-minute token                                                                 |
| GET    | `/api/essays/jobs/:jobId/download`       | The result ZIP                                                                    |
| POST   | `/api/essays/jobs/:jobId/rerun`          | Re-process the same inputs, replacing the result                                  |
| DELETE | `/api/essays/jobs/:jobId`                | Delete the job, its files and its ZIP                                             |

See [Automated Essays](../guides/automated-essays.md).

## Segmenter

The standalone annotation tool. Everything is owner-only.

| Method | Path                                           | Purpose                                                  |
| ------ | ---------------------------------------------- | -------------------------------------------------------- |
| POST   | `/api/segmenter/datasets`                      | Create a dataset                                         |
| GET    | `/api/segmenter/datasets`                      | List datasets                                            |
| GET    | `/api/segmenter/datasets/:id`                  | Dataset with its images and classes                      |
| DELETE | `/api/segmenter/datasets/:id`                  | Delete a dataset                                         |
| POST   | `/api/segmenter/datasets/:id/images`           | Upload images (field `images`)                           |
| GET    | `/api/segmenter/datasets/:id/classes`          | The class palette                                        |
| POST   | `/api/segmenter/datasets/:id/classes`          | Add a class (`name`, `color` as `#RRGGBB`)               |
| PUT    | `/api/segmenter/datasets/:id/classes/:classId` | Rename or recolour                                       |
| DELETE | `/api/segmenter/datasets/:id/classes/:classId` | Delete a class; clears it from the polygons that used it |
| GET    | `/api/segmenter/images/:imageId/file`          | The image bytes                                          |
| DELETE | `/api/segmenter/images/:imageId`               | Delete an image                                          |
| GET    | `/api/segmenter/images/:imageId/annotations`   | The polygons                                             |
| PUT    | `/api/segmenter/images/:imageId/annotations`   | Replace them (max 5000)                                  |

See [Segmenter](../guides/segmenter.md).

## Feedback

| Method | Path            | Purpose                                                                           |
| ------ | --------------- | --------------------------------------------------------------------------------- |
| POST   | `/api/feedback` | Submit a bug report or feature request, with an optional attachment. Rate limited |

## E-mail diagnostics

`/api/test-email/*` — authenticated diagnostics for the SMTP queue
(`test-connection`, `send-test`, `send-direct`, `queue-status`,
`force-queue-process`, `queue-emails`). Operations tooling, not a public API.

---

## What is _not_ here

Documented in older revisions of this tree but **non-existent today**:

- `docs/api/projects.md`, `images.md`, `segmentation.md` — never written; their
  content is the tables above.
- The ML service's `/api/v1/cancel/*` and `/api/v1/jobs/active` endpoints exist
  as source but their router is **not mounted**. Do not call them.

## Related

- [Authentication](authentication.md)
- [WebSocket events](websocket.md)
- [ML service API](ml-service.md)
- [Database schema](../reference/database-schema.md)
- [Backend architecture](../architecture/backend.md)
